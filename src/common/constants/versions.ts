const oldVersion = '0.0.0';
const newVersion = '0.0.1';
export const STABLE_VERSIONS = [oldVersion, newVersion];

export const LATEST_VERSION = [newVersion];

export const LEGACY_VERSION = [oldVersion];
